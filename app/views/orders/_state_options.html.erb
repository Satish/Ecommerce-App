<% if STATE_FOR_STATE_SELECT.include?(country) %>
  <%= state_select object, :state, { :country => get_country_option(country), :selected => state }, {:class => "formField fieldWidth"} -%>
<% else %>
  <%= text_field object, :state, :value => state, :class => "text formField fieldWidth" -%>
<% end %>